import React from 'react';
import PropTypes from 'prop-types';
import { Container, Row } from 'react-bootstrap'

const UploadBox = ({ filesChange }) => (
  <Container>
      asaa
  </Container>
);

UploadBox.propTypes = {
  filesChange: PropTypes.func.isRequired
}

export default UploadBox;
